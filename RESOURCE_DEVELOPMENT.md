# 资源开拓

入口：人才资源库 → 资源开拓（`/resource-management/resource-development`）。

## 业务口径

- 默认显示含今天的最近三个自然日，日期倒序，最新日期自动展开；日期内按更新时间倒序，每页 20 条。日期分页每页 7 天，人员及日期小计始终统计全部筛选结果。
- 新增日期默认为上一个周一至周五工作日，不处理法定节假日、调休。平台、日期、开拓人员和资源姓名必填。
- 一条开拓记录为一个计数单位，并非人才去重人数。每日工时按人员和业务日期汇总，跨平台只计算一次；没有资源记录但已填写工时的日期仍可查询。
- 招呼编号采用平台代码、业务日期和流水号，首次保存生成且永不重编。默认代码示例 `BOSS1`、`ZL1`；自建平台生成稳定的 `P` 前缀代码。流水超过 999 时自然扩展位数；删除记录不回收流水号。
- 微信与企微分别记录联系历史，每次请求有独立日期、人员、账号和累计次数。支持补录、修正和三次以上请求；已保存历史不能删除，只能修正，真实操作者保留在审计表中。
- 当前状态取该渠道最后录入的一条操作。标准“已添加”才会触发人才入库，自定义状态不会触发。已有关联人才后继续修改开拓信息，不会自动覆盖人才档案。

## 权限与人才入库

- 复用人才资源库读写权限（兼容 `translators:read/write`）。普通写入用户只可维护本人名下的记录和工时；超级管理员可管理全部。
- 他人记录的手机号、微信号及可能含联系方式的自由文本返回脱敏值。截图只对本人和超级管理员开放，查重结果不返回人才总库联系方式。
- 查重包含姓名、规范化手机号和微信号。匹配已有档案时由用户确认关联；仅同名可说明不是同一人后新建，手机号或微信号冲突不能绕过。
- 新建人才必须选择专业分类；“全职”映射现有职业档案，笔译/口译/标注映射现有专业能力。语种/方言共享现有字典，方言同时写入方言字段。
- 状态、申请历史和人才创建在一个数据库事务内保存；客户端 UUID 防止重复创建记录，已关联人才防止双渠道重复入库。编辑、删除使用 revision 防止覆盖他人新修改。
- 删除开拓记录不会删除关联人才；操作历史随开拓记录删除，审计保留。

## 日报与截图

- 每日工作支持多个同日时间段，禁止倒序和重叠，扣减不得超过总分钟数；未完成必须填写说明。
- 日报以“人员＋日期”产生稳定来源标识，汇总资源记录数、成功添加数、净分钟数和完成说明。只更新草稿，不更改定稿或已发送快照，不自动发送邮件。
- 日报的资源开拓汇总是不可直接编辑的系统来源行，时长从每日工作派生。手工日报行不受影响；其他系统事件仍为零分钟。
- 截图存储在专用数据库表中，限制 PNG/JPEG/WebP、每张 5MB、最大 2500 万像素。读取接口执行记录所有权检查，并禁用响应缓存。

## 接口与迁移

接口前缀 `/resource-development`：

- `GET /options`、`POST /options`、`PUT /options/{id}`：人员和共享选项、平台说明；`POST /languages`：新增共享语种/方言。
- `GET /days`、`GET /records`：日期和明细分页，复用日期、关键词、人员、平台、账号、状态参数。
- `GET /records/{id}`、`POST /records`、`DELETE /records/{id}?revision=…`：详情、幂等新增/带版本编辑、删除。
- `POST /duplicates`：带所有权校验的查重。
- `GET/PUT /work`：每日工作；`POST /work/{id}/screenshots`、`GET/DELETE /screenshots/{id}`：截图维护。

部署代码前执行 `data/migrations/20260924_resource_development.sql`。迁移可重跑，不覆盖已有配置。新增八张 `resource_development_*` 表，既有人才表无需结构变更。

本次局域网迁移及测试入口：`E:\xinshi_system\.conda_env\python.exe tools\verify_resource_development.py --migrate`。该脚本限定调试机主机名及目录，不能用于生产。省略 `--migrate` 只运行回归。数据库测试逐例回滚，并发编号测试使用独立临时 schema。

前端验证：`npm run build`，以及 `node --test tests/resourceDevelopment.test.mjs tests/talentOverview.test.mjs`。所有运行均在局域网调试机进行。生产发布须另行授权。

## 2026-09-24 跟进交互调整

- 默认列以姓名、语种、开拓人、来源、账号起始，随后展示微信、企微、进群、沟通、入项和最近跟进。招呼编号转为可选字段及详情字段，保留复制能力。
- 点击本人记录的进展单元格打开快捷跟进，自动带入当天日期、当前操作人员与账号；可修改，首次加好友成功仍进行查重入库。
- 进群、沟通、入项使用独立操作类型，允许并存，保留日期、人员及审计。入项是跟进标记，不创建人才档案或项目安排；具体项目可填写后续跟进，本次不建立项目外键关联。
- 保存并继续新增只保留日期、开拓人、来源和账号，清空姓名、联系方式、语种和所有跟进，生成新的请求 UUID。
- 新类型复用原有操作历史表的 varchar 字段，无新增数据库结构迁移。未修改的历史不重写真实修改人和时间。
- 局域网验证：后端86项、前端8项及完整构建通过；最终增量回归9项后端、5项前端通过。浏览器核验连续录入、快捷进群/入项独立保存与详情历史，小屏底部操作保持可见。

## 固定录入员与代录授权

- 新增角色权限 `resource_development:delegate`：可以在开拓模块跨人员录入、修改、查重及查看必要联系方式、每日工作与截图；不授予人才总库联系方式权限，不授予删除他人开拓记录权限。
- 普通用户原所有权限制保持不变。记录开拓人员与真实创建人/修改人分开，日报继续按业务日期和开拓人员汇总。
- 新增表单分为批次信息及资源资料。成功新增后，仅在当前浏览器会话按用户、自然日记忆日期、开拓人、平台和账号；跨日、换用户或失效选项不沿用。姓名、联系方式等个人资料不缓存。
- Ctrl/Command + Enter 在表单内保存，新增态保存后继续录入；提交过程中阻止重复快捷提交，校验失败保留输入。连续新增自动定位姓名。
- 跟进人员默认记录所属开拓人员，可手动修正；同一开拓人员后续新增跟进沿用本轮操作人和操作日期。真实登录录入员仍自动记入审计。
- 局域网已核对 jinghan / 肖景瀚 身份，通过专用角色“资源开拓代录（jinghan）”只添加上述权限，保留原项目经理角色，不影响其他项目经理，未修改生产环境。
- 验证：后端87项、前端15项及完整构建通过，权限测试覆盖跨人员新增/修改、日报归属、删除拒绝、撤权后脱敏及阻止修改。

## 表头筛选与人员汇总

- “来源”统一显示为“开拓平台”，默认紧随序号；保存的字段配置仍按页面固定顺序展示。
- 所有可配置业务列复用 ConfiguredColumnHeaderFilter；枚举多选确认后提交，文本400ms防抖、清空立即查询。列条件与常用筛选以 AND 合并，多选字段内部为 OR。日期分组、明细总数和人员记录小计复用同一后端条件，联系方式和自由文本筛选继续执行所有权/代录权限。
- 当前已启用列筛选以可关闭标签展示，即使无结果或列被隐藏也能清除；重置清除全部条件。
- 人员卡片改为单行摘要＋人员每日汇总浮层。浮层支持人员搜索、限高滚动及对应人员工时入口，人员增多不撑高主表；工时标注为人员当日总工时，不按资源记录筛选拆分。
- 局域网验证：后端88项、前端11项及完整构建通过；使用三条保留样例验证平台筛选、临时选择取消、文本筛选清空、人员搜索及工时归属。

## 2026-09-24 历史数据导入

原表938条，新增935条、既有样例3条；人才总库不变。历史模式由服务端控制，普通RecordWrite不接受该标识；历史导入仅保留标记；后续入库规则见下方“按进入私域触发人才入库”。迁移：data/migrations/20260924_resource_development_history.sql；受控导入工具：tools/import_resource_development_history.py（默认回滚，仅--apply提交，限定局域网主机）。源数据与含联系方式的回执保存在已本地排除的outputs/resource-import-20260924，不提交Git。导入末尾强制验证语种和操作历史关联完整性。

## 2026-09-24 群聊每日新增好友统计

- 群聊统计与求职平台、公共社交平台逐人开拓独立，按日期共享填写；不创建人才档案。日期列表使用上方日期范围，不应用逐人记录的姓名、人员或平台筛选。
- 每天可以录入多行“微信/企微账号＋语种/方言＋当天新增人数”。点击日期或“填写每日统计”打开无背景遮罩的可拖动窗口，支持字段搜索、固定底部保存、未保存提醒及重新加载。
- 账号绑定人才概览稳定列键：带“企微”的 HR 列对应企业微信，其他 HR 列对应普通微信。新增账号会在概览对应分组增加列，已有 HR 账号去重。语种采用现有字典及确定性映射；无匹配时明确选择概览行或新增行，保留语种/方言类型，禁止同日同账号同概览语种重复行。
- 保存共享日报和概览差额在同一数据库事务内完成，采用写锁、日期唯一约束和修订号，重复提交不重复累计；同日修改、移除行扣回原增量。若原单元格已删除或余额不足，不执行不完整更新。空白与明确零分别保留在日报中。
- 写入权限沿用资源开拓代录及超级管理员；其他模块访问者只读。保留真实录入员、修改时间及历次小计审计。
- 新迁移：`data/migrations/20260924_resource_development_friend_daily.sql`。局域网迁移和回归入口：`.conda_env\python.exe tools\verify_resource_friend_daily.py`；依赖现有人才概览快照表。测试使用回滚事务或独立临时 schema，不写入真实统计人数。
- 局域网验收：新增16项后端测试通过，覆盖差额记账、重复提交、并发冲突、日期分页、权限、HTTP保存读取、新方言与账号去重、回滚及原概览手工修改后的保护；相关资源开拓/概览回归与前端15项测试通过。页面已验证普通微信账号映射、语种匹配提示、人数失焦显示、校验、字段搜索定位、拖拽边界与重开复位、历史日期、小屏及固定底部操作。未向真实群聊统计写入测试人数。

## 群聊统计网格录入

填写窗口改为 Excel 风格的双层分组表头：企微在左、微信在右，账号为列、语种为行；每行分渠道小计及底部账号合计自动计算。保留日期切换、字段定位、未保存提醒及固定保存按钮。支持 Tab 横向移动、Enter/上下键同列移动，以及从 Excel 粘贴连续的账号人数区域（不含小计）；非法内容整块拒绝。网格保存仍转换为原有账号明细 API，保留明确零与空白，并沿用差额同步和并发保护。

## 按进入私域触发人才入库

- 微信、企微的“已添加”及进群的“已进群”触发入库；“已邀进群”仅表示邀请，快捷进群默认使用该状态。沟通、入项、自定义状态不触发。
- 前后端比较保存前后的跟进渠道与状态，且要求该渠道最终状态成功。新增成功跟进或修正渠道、状态可以触发；旧标记、备注、日期、人员变更不触发。历史来源继续保留，但不再永久禁止后续入库；导入工具仍不创建人才。
- 新建人才必须填写专业分类并查重，重复候选可确认关联，同名另建需说明，联系方式冲突不可绕过。沿用同一事务、修订号和 person_id 防止重复建档；撤销状态或删除开拓记录不删除人才。
- 保存成功后仍停留在当前资源开拓页面，提示“已入人才总库”和资源编号，详情也展示关联结果。权限、群聊人数独立记账及日报好友成功口径不变。无需迁移。
- 局域网验证：后端41项、前端10项及完整构建、构建体积检查通过。浏览器确认快捷进群默认邀请、确认进群显示入库字段、专业分类必填及自动定位、取消后原状态不变；未将真实记录作为测试人才保存。后端已按交互式计划任务重启并核验会话及 UNC 访问。未发布生产。
