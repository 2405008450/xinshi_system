# 项目协作约定

## 搜索框交互范式

列表筛选、关键字检索和模糊搜索框统一遵循以下规则：

- 用户停止输入 400ms 后自动查询，避免每次按键都请求后端。
- 保留回车查询和“查询”按钮，供用户主动触发。
- 输入框清空时立即恢复完整列表，不等待防抖计时。
- 新查询开始时取消上一条未完成请求，并使用请求序号防止旧响应覆盖新结果。
- 自动查询时将页码重置为第一页；分页、列表和总数统计必须使用同一组筛选条件。
- 后端模糊搜索应覆盖界面提示的全部业务字段，例如“客户名称”同时匹配客户全称和简称。
- 该范式只用于列表查询类搜索框；表单录入、远程下拉和自动补全控件应根据交互需求单独设计。
- 数据量明显增长后，应结合数据库类型评估全文检索、trigram 或其他适合包含匹配的索引，避免仅依赖 `%关键词%` 扫描。

## 查询表单与高级筛选范式

列表页面存在多个查询字段时，统一采用“常用条件常驻 + 高级条件悬浮”的布局：

- 查询栏只常驻客户名称、业务状态等 2～3 个最高频条件，以及“查询”“重置”“高级筛选”按钮。
- 低频、组合型条件统一放入 Element Plus `el-popover` 悬浮小窗；不得使用会撑高页面的折叠面板，避免减少表格在当前视口中的可见行数。
- 高级筛选默认使用 `trigger="click"`、`placement="bottom-end"`，参考宽度为 `760px`；宽度必须受视口限制，小屏幕下不得溢出页面。
- 悬浮小窗内容较多时只允许小窗内部纵向滚动，建议最大高度为 `min(560px, calc(100vh - 120px))`，不得带动主页面布局变化。
- “高级筛选”按钮应显示当前已启用的高级条件数量；小窗内提供“清空高级条件”和“关闭”，点击窗口外部也应能够关闭。
- 关闭小窗只隐藏界面，不清除已选条件；“重置”清除常用及高级条件，“清空高级条件”只清除高级条件。
- 文本型条件继续遵循 400ms 防抖、回车查询、请求取消和旧响应保护；日期、人员、状态等选择型条件变更后可以立即查询。
- 自动或手动执行查询时统一回到第一页，列表接口和总数接口必须复用同一份筛选参数构造逻辑。
- 多个筛选条件默认使用 AND 组合；单个业务字段需要兼容多个历史值时，由后端在该字段内部使用 OR 或别名映射处理。
- 新增或修改高级筛选后，应验证悬浮小窗定位、内部滚动、条件计数、重置行为以及主表格高度不受影响。

## 列表详情展示范式

当业务对象字段较多、列表无法完整展示时，统一遵循以下规则：

- 主表格只展示高频、便于快速识别和筛选的关键字段，避免为了展示全部信息而持续增加表格宽度。
- 在表格右侧增加独立的“详情”列，并提供文字型“查看详情”按钮；“详情”列与“操作”列分开，均固定在右侧。
- “查看详情”统一使用 Element Plus 的 `el-popover` 小窗，不使用跳转页面或 `el-dialog` 大弹窗替代。
- 小窗统一使用 `trigger="click"`、`placement="left"`、`:width="760"`，标题采用“业务对象名称 + 详情”，例如“客户详情”。
- 小窗内容统一使用 `el-descriptions`，配置为 `:column="2"`、`border`、`size="small"`。
- 详情内容区域最大高度为 `560px`，内容超出时允许纵向滚动；长文本必须自动换行，空值统一显示 `-`。
- 日期时间字段统一格式化为中文日期时间，不直接展示未经处理的接口时间字符串。
- 列表接口未返回完整字段时，在小窗打开时调用详情接口按需加载，并展示 loading 状态；已经加载的数据可以按记录 ID 缓存。
- 数据编辑或删除成功后，必须清除对应记录的详情缓存，防止再次打开时展示旧数据。
- 同类详情小窗出现于两个及以上页面时，应优先抽取公共组件或公共样式，不得在多个页面中长期复制并独立维护相同实现。
- 新增或修改详情小窗后，除构建检查外，还应在实际页面点击“查看详情”，确认生成的是 `el-popover`、弹出方向和内容滚动均符合规范。

## 长表单弹窗交互范式

新增、编辑等表单内容可能超过当前视口高度时，统一采用“固定头部与底部操作栏 + 中间内容独立滚动”的布局，参考 `/consultations` 的表单弹窗实现：

- 表单使用 Element Plus `el-dialog`；弹窗宽度必须受视口限制，例如 `width="min(960px, calc(100vw - 32px))"`，并建议设置 `top="5vh"`。
- 弹窗根节点采用纵向 flex 布局，最大高度为 `90vh`，设置 `overflow: hidden`；不得让整个弹窗或页面随长表单继续向下延伸。
- `el-dialog__header` 和 `el-dialog__footer` 设置为不可压缩区域，`el-dialog__body` 使用 `flex: 1`、`min-height: 0`、`overflow-y: auto`，只允许表单内容区域纵向滚动。
- “取消”“确定/保存”等主要操作必须放在 `#footer` 插槽中，并在弹窗打开及内容滚动到任意位置时始终可见；不得要求用户滚动到表单末尾才能提交或取消。
- 不得在 `el-dialog__body` 内再用包裹整个表单的 `el-scrollbar max-height="..."` 制造嵌套滚动；仅局部表格、树或选项列表确有独立滚动需求时才使用内部滚动容器。
- 底部操作栏应使用与主体区分的背景、上边框或轻微阴影，确保在内容滚动时仍能明确识别为固定操作区域。
- 小屏幕下弹窗仍不得溢出视口，多列表单应切换为单列或适合当前宽度的布局，同时保留固定底部操作栏。
- 表单校验失败时，应保持底部操作栏可见，并将内容区滚动到首个错误字段附近，不得改变弹窗整体在视口中的位置。
- 新增或修改长表单弹窗后，除构建检查外，还必须分别打开“新增”和“编辑”状态，验证初始视图、滚动到中部和底部时操作栏始终可见，并确认只有内容区发生滚动。

## 只读与自动生成字段范式

新增和编辑表单中，订单号、客户编号、从关联对象带出的字段需要占位展示但不允许用户修改。这类字段统一遵循「白底表示待填写，灰底表示系统给值」：

- 表单字段只有「可编辑」和「只读」两种视觉状态，分别使用白底 `#FFFFFF` 和灰底 `#F1F5F9`；不得引入第三种底色，也不得让可编辑字段使用灰色填充底。
- 只读字段统一使用 `frontend/src/components/common/ReadonlyField.vue`，不在页面内重复实现 `.readonly-input` 之类的局部样式；该组件封装了底色、文字色、图标、光标和 `readonly`/`disabled` 的选择。
- 系统生成或从关联对象自动带出的字段使用 `source="auto"`，底层为 `readonly`，显示魔棒图标。这类字段必须保留用户选中复制的能力——订单号、客户编号正是用户要复制到邮件和表格中的值，不得使用 `disabled`。
- 前置条件未满足、或编辑态下不允许修改的键值字段使用 `source="locked"`，底层为 `disabled`，显示锁图标，Tab 键应跳过。
- 只读状态必须同时具备灰底、图标和文案三重表达，不得只依靠颜色区分。
- 只读字段一律使用 `:model-value` 单向绑定，不使用 `v-model`；自动计算字段不得把 `v-model` 绑到没有 setter 的 `computed`。
- 非 `el-input` 控件的 `readonly` 语义不可靠，`el-date-picker` 加 `readonly` 后选择面板仍会弹出。这类控件（`el-date-picker`、`el-select`、`el-input-number`）只读时必须使用 `disabled`，或改用 `ReadonlyField` 渲染格式化后的文本。
- 创建人、创建时间、更新时间等系统字段不进入编辑表单，统一在 `el-descriptions` 或详情小窗展示。
- 提示文案按场景收敛为三类：保存后由后端生成用「保存后自动生成」，选择关联对象后带出用「选择客户后自动带出」，前置条件未满足用「请先选择客户」。
- 「留空则自动生成、但允许手动填写」的编号字段属于可编辑状态，使用白底普通输入框和「自动生成或手动输入」提示，不得误用只读样式。
- 必填标记统一由 `el-form` 的 `:rules` 配合 `el-form-item` 的 `prop` 驱动，不在 label 文本中手写星号，也不只在 placeholder 里写「（必填）」。
- 色值、对比度核算及「不得全局加深 `--el-disabled-text-color`」的原因参见 `frontend/UI_GUIDELINES.md` 第 5.4 节。
- 新增或修改只读字段后，应分别打开「新增」和「编辑」状态，确认新增态显示提示文案、编辑态显示真实值且清晰可读，订单号可以选中复制，锁定字段 Tab 键跳过，并对比同一字段在不同模块中的表现是否一致。

## 列表字段自定义范式

当业务对象可展示字段较多、不同用户关注字段不一致时，统一提供用户可配置的列表字段：

- 页面必须先定义一组精简的默认字段，只包含高频、便于快速识别和判断状态的信息；低频字段默认放入“查看详情”小窗。
- 在列表页标题操作区提供“字段设置”入口，位置应靠近“新增”等页面级操作按钮。
- “字段设置”使用 `el-popover` 和复选框列表，用户勾选或取消后，主表格应立即更新，无需重新请求业务数据。
- 可选字段应覆盖该业务对象适合在表格中展示的全部字段；长文本字段启用溢出提示，日期时间和枚举字段必须沿用页面统一的格式化展示。
- 序号、展开控制、详情、操作等结构性列不参与用户配置，必须始终保留；业务字段允许用户全部取消。
- 字段顺序由页面统一配置决定，用户选择只控制显示与隐藏，避免不同用户产生不可维护的任意列顺序。
- 用户选择必须按“页面/业务模块 + 当前用户”隔离并持久化到 `localStorage`，刷新和重新进入页面后继续生效，不得影响其他用户。
- 读取历史配置时必须过滤已经删除或重命名的字段键；配置不存在或解析失败时使用默认字段并清理无效数据。
- 字段设置小窗必须提供“恢复默认”，一键还原页面预设字段组合。
- 字段增加较多导致总宽度超过容器时，允许表格横向滚动，但右侧“详情”和“操作”列仍需固定。
- 新增或修改字段设置后，应验证默认字段、自由勾选、全部取消、恢复默认、刷新后保留以及不同用户配置隔离。

## 列表删除管理范式

业务列表存在删除能力时，统一使用页面级“删除管理”，不得在每条记录的操作列中长期放置独立删除按钮：

- “删除管理”放在列表标题操作区，与“新增”“字段设置”等页面级操作相邻；优先复用 `BatchDeleteToolbar` 与 `useBatchDelete`。
- 默认状态不显示表格勾选列，也不在逐条操作列中显示“删除”；点击“删除管理”后进入删除模式并显示勾选列。
- 删除模式必须显示已选数量，并提供“退出”和“删除所选”；未选择记录时“删除所选”必须禁用。
- 删除模式下隐藏逐条操作列以及新增、批量编辑等可能造成模式冲突的入口；查看详情等只读能力可以保留。
- 只选一条时使用普通二次确认；选择多条时必须采用更强确认（例如要求输入“删除”），明确提示不可恢复。
- 批量删除应逐条记录成功与失败，成功项及时刷新，失败项保留选择并展示具体原因，不得因单条失败掩盖其他记录的结果。
- 删除成功后清理对应详情及关联缓存；若当前页已无数据，应自动修正到有效页码。
- 切换分页时退出删除模式并清空选择，避免用户误删不同分页中的记录。
- 表单内部的动态明细行、“平台管理”等局部子资源不属于业务主列表，可按其上下文保留行内删除。
- 新增或修改列表删除能力后，应验证进入/退出模式、单条删除、多条删除、部分失败、末页回退及缓存清理。
- 完整说明参见 `docs/前端交互规范.md`。
