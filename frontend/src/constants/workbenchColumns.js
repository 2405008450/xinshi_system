// 工作台管理层与执行层共享列宽，确保上下两张表的公共字段严格对齐。
export const WORKBENCH_COLUMN_WIDTHS = Object.freeze({
  selection: 44,
  index: 50,
  orderNo: 145,
  projectTask: 320,
  client: 100,
  customerDeadline: 145,
  projectStatus: 100,
  currentAssignee: 110,
  languagePair: 105,
  difficulty: 68,
})
