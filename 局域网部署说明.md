# 局域网部署说明

## 网络配置信息

- 服务器 IP：`192.168.31.144`
- 项目目录：`E:\xinshi_system`
- Conda 环境：`E:\xinshi_system\.conda_env`
- 子网掩码：`255.255.255.0`
- 默认网关：`192.168.31.1`

## 正式访问地址

- 员工统一通过 `https://<公司内部域名>` 访问。
- 内网 DNS 必须将该域名解析到 `192.168.31.144`。
- `http://192.168.31.144:3000` 仅用于开发调试，不能作为桌面通知的正式入口。
- 不要混用 IP 和域名，否则登录状态和通知授权会按不同来源隔离。

浏览器系统通知要求安全上下文。除 `localhost` 外，普通 HTTP IP 地址不能申请通知权限。

## 启动后端

项目使用已经配置好的 Conda 环境，不使用 Docker。进入项目根目录后双击 `start_backend.bat`，或执行：

```powershell
Set-Location -LiteralPath 'E:\xinshi_system'
& '.\.conda_env\python.exe' -m uvicorn main:app --host 0.0.0.0 --port 8000 --reload
```

生产环境不要使用 `--reload`，并应限制 8000 端口只允许本机或受信任网段访问。

以上直接启动方式只适用于服务器本机已登录的 `Administrator` 桌面会话。后端需要继承该交互式会话的 SMB 凭据以读取 `\\Win-server` 共享目录；通过 SSH 远程维护时，必须使用 `XinshiDebugBackendInteractive` 计划任务启动，禁止使用 WMI/CIM、SSH `Start-Process`、Windows 服务或其他 Session 0 启动方式。

远程重启前后必须按 [项目环境与操作边界](docs/infra.md#局域网启动方式) 核对活动控制台会话、计划任务 `LogonType`、后端进程 `SessionId`，并在相同交互式上下文中完成一次实际 UNC 目录只读枚举。没有活动桌面会话或验证失败时不得降级到 Session 0。

## 开发环境启动前端

在 `frontend` 目录运行：

```powershell
Set-Location -LiteralPath 'E:\xinshi_system\frontend'
npm install
npm run dev -- --host 0.0.0.0 --port 3000
```

开发地址只用于调试；其他员工应使用正式 HTTPS 域名。

## 构建正式前端

```bash
cd frontend
npm install
npm run build
sudo cp -r dist/* /var/www/html/
```

## 配置局域网 HTTPS

1. 为可控域名申请受员工电脑信任的 HTTPS 证书。
2. 在内网 DNS 中将该域名解析到 `192.168.31.144`。
3. 使用模板生成 Nginx 配置，以下变量必须替换为真实值：

```bash
export XINSHI_SERVER_NAME="oa.example.com"
export XINSHI_CERTIFICATE_PATH="/etc/letsencrypt/live/oa.example.com/fullchain.pem"
export XINSHI_CERTIFICATE_KEY_PATH="/etc/letsencrypt/live/oa.example.com/privkey.pem"
envsubst '$XINSHI_SERVER_NAME $XINSHI_CERTIFICATE_PATH $XINSHI_CERTIFICATE_KEY_PATH' \
  < deploy/nginx-https.conf.template \
  | sudo tee /etc/nginx/sites-available/xinshi-system >/dev/null
sudo ln -sfn /etc/nginx/sites-available/xinshi-system /etc/nginx/sites-enabled/xinshi-system
sudo nginx -t
sudo systemctl reload nginx
```

模板会将 HTTP 自动重定向到 HTTPS，并保留 `/api/` 与 `/api/notifications/ws` 的反向代理。

## 防火墙设置

允许局域网客户端访问：

- TCP 80：仅用于跳转 HTTPS。
- TCP 443：正式系统入口。
- TCP 8000：建议不直接对普通客户端开放。

## 桌面通知启用与验证

1. 员工打开 HTTPS 域名并登录。
2. 点击右上角通知铃铛中的“启用”，在浏览器授权框中选择“允许”。
3. 确认 Windows“设置 → 系统 → 通知”中已允许 Edge 或 Chrome 显示通知。
4. 触发一条交接或项目通知，确认 Windows 通知中心出现通知。
5. 在开发者工具 Network 中确认 `/api/notifications/ws` 返回 `101 Switching Protocols`，连接协议为 `wss://`。
6. 点击系统通知，确认窗口获得焦点、通知标记已读并进入对应业务页面。

## 注意事项

1. 保持服务器 IP 固定，并同步维护内网 DNS 记录。
2. HTTPS 证书续期后必须重新加载 Nginx，建议配置证书工具的自动续期钩子。
3. Windows 勿扰模式可能使通知静默，但不会影响站内通知记录。
4. 浏览器完全退出后，本阶段不会继续接收通知；网页打开或最小化时可以接收。
